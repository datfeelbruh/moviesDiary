base-url: /api

spring:
  application:
    name: movies_diary

  jpa:
    show-sql: true

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog-master.yaml

  mvc:
    hiddenmethod:
      filter:
        enabled: true

logging:
  level:
    root: INFO